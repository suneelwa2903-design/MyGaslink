# Architecture — Re-New GasLink

> Generated by `/onboard` on 2026-05-04 from codebase scan. Items marked `⚠️ [NEEDS_HUMAN_INPUT]` need confirmation before relying on.

## 1. System Overview

LPG distribution management SaaS. Multi-tenant (one tenant = one **Distributor**), with a public landing/contact site, an admin/operator web app, a customer self-service portal, and a mobile app for drivers, inventory staff, finance, and admins.

**Monorepo (pnpm workspaces, Node ≥20):**
- `packages/api` — Express 5 + TypeScript + Prisma 6 (PostgreSQL 17)
- `packages/web` — React 19 + Vite 7 + Tailwind + Zustand + React Query + react-router 7
- `packages/mobile` — Expo 54 + React Native 0.81 + expo-router + NativeWind + Zustand
- `packages/shared` — Cross-cutting types and enums (consumed by all three)

## 2. Tech Stack (actual versions)

| Layer | Technology | Version |
|------|-----------|---------|
| Runtime | Node.js | ≥20 |
| Package manager | pnpm | 9.15.0 |
| Backend framework | Express | ^5.1.0 |
| ORM | Prisma | ^6.19.2 |
| Database | PostgreSQL | 17-alpine (Docker) — port `5433` host → `5432` container |
| Auth | jsonwebtoken + bcryptjs | jwt 9.0.2, bcryptjs 2.4.3 |
| Validation | Zod | ^3.25 |
| Logger | Winston | ^3.18 |
| Error tracking | Sentry (Node SDK) | ^10.45 |
| Email | nodemailer | ^7.0 |
| PDF | pdfkit + qrcode + sharp | — |
| Web framework | React | ^19.1 |
| Web build | Vite | ^7.0 |
| Web state | Zustand + React Query (TanStack v5) | zustand 5.0, RQ 5.80 |
| Web styling | Tailwind 3.4 + framer-motion 12 + react-hot-toast | — |
| Web HTTP | axios | ^1.10 |
| Mobile | Expo + React Native | expo 54, RN 0.81 |
| Mobile router | expo-router | ~6.0 |
| Mobile secure storage | expo-secure-store | ~15.0 |
| Tests | Vitest (api/web), Jest (mobile) | vitest ^3.2, jest (mobile) |
| CI | GitHub Actions | `.github/workflows/ci.yml`, `e2e-monitor.yml` |

## 3. Project Layout

```
Re-New_Gaslink/
├── packages/
│   ├── api/
│   │   ├── prisma/        schema.prisma (1406 lines, 45 models, 38+ enums), seed.ts, migrations/
│   │   ├── src/
│   │   │   ├── routes/        22 route files
│   │   │   ├── services/      ~17 services + gst/ + pdf/ subfolders
│   │   │   ├── middleware/    auth.ts, validate.ts, requestId.ts, auditLog.ts
│   │   │   ├── lib/, config/, utils/, __tests__/
│   │   │   ├── app.ts, server.ts, swagger.ts
│   │   ├── test-*.ts          ⚠️ ad-hoc test scripts at package root (see Gaps)
│   ├── web/src/
│   │   ├── pages/             25 page components (admin + customer/)
│   │   ├── components/        layout/, ui/
│   │   ├── routes/            ProtectedRoute.tsx, index.tsx
│   │   ├── stores/            authStore.ts, themeStore.ts (Zustand)
│   │   ├── lib/, services/, hooks/, __tests__/
│   ├── mobile/
│   │   ├── app/               expo-router file-based routing
│   │   │   ├── (admin)/, (auth)/, (customer)/, (driver)/, (finance)/, (inventory)/, (super-admin)/
│   │   ├── src/               components, services, stores, lib, theme
│   └── shared/                cross-package types/enums
├── docs/                      E2E_Testing_Guide.xlsx, Navigation_Smoke_Test.xlsx, TESTING_PROGRESS.md
├── docker-compose.yml         Dev DB
├── docker-compose.prod.yml    Prod stack
├── scripts/                   backup.sh, restore.sh, e2e_test.js
└── CLAUDE.md, MASTER_ASSESSMENT_REPORT.md
```

## 4. Authentication & Authorization

**Mechanism:** JWT — access token (15m) + refresh token (7d), HMAC-signed with separate secrets (`JWT_ACCESS_SECRET`, `JWT_REFRESH_SECRET`). Tokens are stored client-side in Zustand + localStorage (web) / expo-secure-store (mobile).

**Roles** (enum `UserRole` in [schema.prisma:14](packages/api/prisma/schema.prisma:14)):
- `super_admin` — platform-level; bypasses role checks; selects active tenant via `X-Distributor-Id` header
- `distributor_admin`
- `finance`
- `inventory`
- `driver`
- `customer`

**Auth middleware chain** ([packages/api/src/middleware/auth.ts](packages/api/src/middleware/auth.ts)):
1. `authenticate` — verifies JWT, loads user from DB, checks `status === 'active'`, populates `req.user`
2. `resolveDistributor` — for super_admin, reads `X-Distributor-Id` header (validated against `/^[a-zA-Z0-9_-]{1,128}$/`); for other roles, takes `distributorId` from JWT
3. `requireDistributor` — hard-fails any request without a resolved `distributorId`
4. `requireRole(...roles)` — per-route role gate (super_admin auto-passes)

## 5. Multi-Tenancy (CRITICAL)

**Model:** Single shared database, single shared schema, **`distributor_id` discriminator column** on every tenant-scoped table. 29 models carry a `distributor_id` FK to the `distributors` table.

**Tenant root:** `Distributor` ([schema.prisma:315](packages/api/prisma/schema.prisma:315)) — owns customers, orders, invoices, payments, drivers, vehicles, cylinder types/prices, inventory events/summaries, settings, billing cycles, pending actions, accountability logs, audit logs, licenses, GST credentials.

**Per-tenant config that changes app behaviour:**
- `gstMode`: `disabled | sandbox | live` — controls whether GST e-invoice/e-waybill flows run
- `subscriptionPlan`, `billingTier`, `billingSuspended`, `gaslinkBillingEnabled` — SaaS-billing controls
- `providerCodes[]` — IOCL/HPCL/BPCL provider linkage

**Isolation enforcement:**
- All distributor-scoped routes mounted with `authenticate → resolveDistributor → requireDistributor` ([app.ts:84-101](packages/api/src/app.ts:84))
- Services must filter every query by `req.user.distributorId`. ⚠️ This is a **convention**, not enforced by Prisma row-level security. Any new query that omits the filter is a tenant-leak bug.
- Super admin elevates to a tenant by sending `X-Distributor-Id` — never trust the body for this.

**Platform-level routes (no `requireDistributor`):**
- `/api/distributors` — super_admin CRUD over tenants
- `/api/health` — liveness/readiness
- `/api/provider-catalog` — global cylinder catalog (cross-tenant reference data)
- `/api/billing` — has `resolveDistributor` but not `requireDistributor` (super_admin can list all-tenants billing) ⚠️ [NEEDS_HUMAN_INPUT: confirm intended]
- `/api/pricing` — same shape as billing ⚠️ [NEEDS_HUMAN_INPUT: confirm intended]
- `/api/users` — same shape ⚠️ [NEEDS_HUMAN_INPUT: confirm intended]

## 6. Database

- **Engine:** PostgreSQL 17
- **ORM:** Prisma 6.19, code-first schema at [packages/api/prisma/schema.prisma](packages/api/prisma/schema.prisma) (1406 lines)
- **Models:** 45 (Distributor, User, Customer + CustomerContact/Discount/InventoryBalance/ModificationRequest/AuditTrail/LedgerEntry, CylinderType/Price/EmptyPrice/Threshold, Order/OrderItem/OrderStatusLog, Driver, Vehicle, DriverVehicleAssignment, DriverAssignment, VehicleInventory, Invoice/Item, CreditNote, DebitNote, PaymentTransaction, PaymentAllocation, InventoryEvent, InventorySummary, CancelledStockEvent, GstDocument, GstCredential, BillingCycle/Item, PricingTier, GstApiUsage, SeatRequest, PendingAction, AccountabilityLog, AuditLog, DistributorSetting, License, ContactSubmission, GstState, HsnCode, ProviderCatalogCylinderType)
- **Enums:** 38+ (UserRole, UserStatus, ProvisioningStatus, DistributorStatus, GstMode, CustomerStatus, OrderStatus, OrderType, InvoiceStatus, IrnStatus, EwbStatus, GstDocType, DriverStatus, VehicleStatus, AssignmentStatus, …)
- **Soft deletes:** `deletedAt` on Distributor/User/Customer (and likely others — convention)
- **Migrations:** ⚠️ Only one migration on disk: `20260323000000_init`. Either the schema is stable since baseline, or migrations have been squashed/reset. Going forward, all schema changes should be incremental migrations. [NEEDS_HUMAN_INPUT: confirm migration policy]
- **Seed:** [packages/api/prisma/seed.ts](packages/api/prisma/seed.ts) — populates the 7 test accounts + 2 distributors + cylinder catalog

## 7. API Surface

22 router files mounted under `/api/*`:

| Mount | Router | Tenant-scoped |
|------|--------|---------------|
| `/api/auth` | `auth.ts` | public |
| `/api/health` | `health.ts` | public |
| `/api/contact` | `contact.ts` | public |
| `/api/users` | `users.ts` | resolve only |
| `/api/distributors` | `distributors.ts` | platform |
| `/api/customers` | `customers.ts` | yes |
| `/api/cylinder-types` | `cylinderTypes.ts` | yes |
| `/api/orders` | `orders.ts` | yes |
| `/api/inventory` | `inventory.ts` | yes |
| `/api/invoices` | `invoices.ts` | yes |
| `/api/payments` | `payments.ts` | yes |
| `/api/drivers`, `/api/vehicles` | `driversVehicles.ts` | yes |
| `/api/analytics` | `analytics.ts` | yes |
| `/api/settings` | `settings.ts` | yes |
| `/api/pending-actions` | `pendingActions.ts` | yes |
| `/api/billing` | `billing.ts` | resolve only |
| `/api/accountability` | `accountability.ts` | yes |
| `/api/customer-portal` | `customerPortal.ts` | yes |
| `/api/delivery` | `deliveryWorkflow.ts` | yes |
| `/api/assignments` | `assignments.ts` | yes |
| `/api/provider-catalog` | `providerCatalog.ts` | platform |
| `/api/pricing` | `pricing.ts` | resolve only |

**Standard response envelope** ([packages/api/src/utils/apiResponse.ts](packages/api/src/utils/apiResponse.ts)):
`{ success: boolean, data: T \| null, error: string \| null, code?: string }`

**Other standards:**
- Helmet + per-window rate limit (1000 req / 15 min) + CORS allowlist
- Request ID middleware → propagated as `X-Request-Id`
- Sentry on unhandled errors
- Body limit 10mb (raised for PDF/photo payloads)
- Swagger UI gated to super_admin

## 8. External Integrations

| Service | Purpose | Config |
|---------|---------|--------|
| **WhiteBooks GST API** | E-invoice (IRN) and E-waybill issuance via GasLink-level credentials | `GASLINK_GST_*`, sandbox/live toggle |
| **SMTP (Gmail by default)** | Contact form, password reset, notifications | `SMTP_*` |
| **AWS S3 + CloudFront** | Asset storage and CDN (prod) | `AWS_*` |
| **Sentry** | Error monitoring | (via `lib/sentry.ts`) |
| **WhatsApp Business** | Future — env vars stubbed | `WHATSAPP_*` |

## 9. Frontend (Web) — `packages/web`

- **Routing:** [packages/web/src/routes/index.tsx](packages/web/src/routes/index.tsx) with `ProtectedRoute.tsx` for role-gated routes
- **Layout:** `DashboardLayout.tsx`, `Sidebar.tsx`, `DistributorSelector.tsx` (super_admin tenant switcher), `ThemeToggle.tsx`
- **State:** Zustand stores — `authStore.ts` (token + user), `themeStore.ts`
- **Server state:** TanStack Query (`@tanstack/react-query` v5)
- **Forms:** react-hook-form + Zod via `@hookform/resolvers`
- **HTTP:** axios with interceptors that inject `Authorization: Bearer <token>` and `X-Distributor-Id` (super_admin)
- **Pages:** 25 — Analytics, Orders, Inventory, Customers, BillingPayments, Fleet, Collections, Settings, Distributors, ProviderCatalog, HealthMonitoring, customer-portal subset

## 10. Frontend (Mobile) — `packages/mobile`

- **Routing:** expo-router file-based, with role-segmented groups: `(admin)/ (auth)/ (customer)/ (driver)/ (finance)/ (inventory)/ (super-admin)/`
- **Storage:** expo-secure-store for the JWT
- **Camera:** `expo-camera` (delivery photo capture)
- **Location:** `expo-location` (driver tracking)
- **Notifications:** `expo-notifications`
- **Styling:** NativeWind (Tailwind for RN)

## 11. Tests

- **API integration tests** (Vitest, supertest): 6 files in [packages/api/src/__tests__/](packages/api/src/__tests__/) — auth, customer-portal, gst-invoicing, gst-toggle, inventory, workflow. **94/94 passing as of 2026-04-07.**
- **Web tests:** Vitest configured; only smoke coverage today. ⚠️ [NEEDS_HUMAN_INPUT: confirm web test policy]
- **Mobile tests:** Jest configured (`jest.config.js`). ⚠️ [NEEDS_HUMAN_INPUT: confirm coverage]
- **Manual E2E suite:** `docs/E2E_Testing_Guide.xlsx` (272 cases), `docs/Navigation_Smoke_Test.xlsx` (55 cases). Tracked in `docs/TESTING_PROGRESS.md`. Currently in Phase 1 (smoke, 0/55 done) → Phase 4 (API tests, ✅ done).

## 12. CI / Infra

- **CI:** `.github/workflows/ci.yml` and `e2e-monitor.yml` ⚠️ [NEEDS_HUMAN_INPUT: confirm what each does]
- **Local dev:** `pnpm docker:up` for postgres, then `pnpm dev` runs api+web in parallel
- **Prod stack:** `docker-compose.prod.yml`
- **Backup/restore:** `scripts/backup.sh`, `scripts/restore.sh`
- **Production hosting target:** ⚠️ [NEEDS_HUMAN_INPUT — AWS based on env vars, but exact runtime (ECS/EC2/EKS) not declared]

## 13. Key Domain Rules

- Order lifecycle: `pending_driver_assignment → pending_dispatch → pending_delivery → delivered | modified_delivered | cancelled | returns_only`
- Invoice lifecycle: `draft → issued → partially_paid | paid | overdue | cancelled`
- IRN lifecycle: `not_attempted → pending → success | failed | cancelled` (only attempted when distributor.gstMode != disabled)
- E-waybill lifecycle: `not_attempted → pending → active | failed | cancelled`
- Driver-vehicle assignment lifecycle: `dispatch_ready → loaded_and_dispatched → returned_inventory → reconciled | cancelled`
- Customer can be `active | suspended | inactive` — `suspended` blocks new orders
- Inventory days can be locked once reconciled (`InventorySummary`)
- Cancelled stock returns to free inventory via `CancelledStockEvent`

## 14. Known Risks / Outstanding Work

(See `gap-report.md` for the full list and `docs/TESTING_PROGRESS.md` for live bug list.)

- Multi-tenant isolation is convention-based — every new query is a potential leak.
- Single migration on disk → discipline needed going forward.
- Ad-hoc test scripts (`packages/api/test-*.ts`) sit at the package root, are not part of the Vitest suite, and are not documented.
